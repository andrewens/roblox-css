return {
    CLASS_ATTRIBUTE_NAME = "class", -- this attribute is reserved for defining an Instance's custom classes it belongs to
    CLASS_SEPARATOR_SYMBOL = " ", -- this char separates individual custom classes in the class attribute
    CUSTOM_CLASS_SYMBOL = "_", -- distinguishes between RbxClasses & CustomClasses in the Stylesheet table parameter to mount()
    STYLESHEET_FILE_EXTENSION = ".rcss" -- only module scripts with this at the end of their name are considered stylesheets
}
